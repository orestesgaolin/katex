/// Server/stub variant: no Flutter on the server, so the widget is null.
/// The real implementation lives in `math_cell_builder_web.dart`.
library;

// ignore: avoid_annotating_with_dynamic
dynamic mathCellWidget(String tex, {required bool displayMode}) => null;
