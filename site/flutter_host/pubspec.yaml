name: flutter_host
description: "Flutter-web host that renders the katex_flutter Math gallery for embedding in the comparison site (T-024)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  katex_flutter:
    path: ../../packages/katex_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
